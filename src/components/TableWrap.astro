---
// Wraps MDX/Markdown tables in a horizontally-scrollable container so wide data
// tables (e.g. the deployment cadence) never overflow the article column.
---
<div class="table-scroll">
  <table><slot /></table>
</div>
